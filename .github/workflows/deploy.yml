name: Deploy Docusaurus to GitHub Pages

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build Docusaurus and QA report
    runs-on: ubuntu-latest

    outputs:
      qa-outcome: ${{ steps.docs-qa.outcome }}
      qa-report-path: ${{ steps.add-qa-report.outputs.report-path }}
      qa-report-version: ${{ steps.add-qa-report.outputs.report-version }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      # Build the site before using AI credits. If the Docusaurus build
      # itself is broken, the workflow stops here.
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Install site dependencies
        run: npm ci

      - name: Build Docusaurus site
        env:
          # Expose the deployment version to Docusaurus. The QA route itself also
          # uses a versioned report file below, so this is safe even if the site
          # configuration does not currently consume QA_VERSION.
          QA_VERSION: ${{ github.sha }}
        run: npm run build

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install documentation QA dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-doc-review.txt

      # The Python QA script invokes Vale directly and parses its JSON
      # output into the unified documentation QA report. Vale uses
      # mdx2vast as an external parser for MDX files.
      #
      # Install Vale as a normal Linux binary rather than through Snap.
      # A Snap-confined Vale process may not be able to execute the
      # globally installed mdx2vast binary even when the Actions shell
      # itself can find mdx2vast in PATH.
      - name: Install Vale and MDX support
        shell: bash
        run: |
          VALE_VERSION="3.17.1"
          curl -fsSL \
            "https://github.com/vale-cli/vale/releases/download/v${VALE_VERSION}/vale_${VALE_VERSION}_Linux_64-bit.tar.gz" \
            -o /tmp/vale.tar.gz

          mkdir -p /tmp/vale
          tar -xzf /tmp/vale.tar.gz -C /tmp/vale
          sudo install /tmp/vale/vale /usr/local/bin/vale

          # Vale requires mdx2vast to lint MDX files.
          npm install --global mdx2vast

          echo "Vale:"
          command -v vale
          vale --version

          echo "mdx2vast:"
          command -v mdx2vast

      # Validate the repository Vale configuration and MDX parser before
      # running the documentation QA script.
      - name: Validate Vale configuration
        shell: bash
        run: |
          if [ ! -f ".vale.ini" ]; then
            echo "::error::.vale.ini was not found at the repository root."
            exit 1
          fi

          if ! command -v vale >/dev/null 2>&1; then
            echo "::error::Vale was not found in PATH."
            exit 1
          fi

          if ! command -v mdx2vast >/dev/null 2>&1; then
            echo "::error::mdx2vast was not found in PATH. Vale cannot lint MDX files."
            exit 1
          fi

          echo "Vale configuration found."
          echo "Vale: $(command -v vale)"
          echo "mdx2vast: $(command -v mdx2vast)"

          vale sync
          vale ls-config

          # Exercise Vale against a real MDX file before the documentation
          # QA step consumes API credits. This catches parser/runtime issues
          # such as "mdx2vast not found" during the preflight.
          TEST_MDX="$(find . -type f -name '*.mdx' -not -path './node_modules/*' | head -n 1)"

          if [ -n "$TEST_MDX" ]; then
            echo "Testing Vale MDX parsing with: $TEST_MDX"
            vale --output=JSON --no-exit "$TEST_MDX" >/tmp/vale-mdx-preflight.json
            echo "Vale MDX parsing: OK"
          else
            echo "No MDX files found; skipping the MDX parser preflight."
          fi

      # LTeX+ 18.7.0 requires Java 21.
      #
      # Explicitly install/select Java 21 so the LTeX+ launcher does not
      # fall back to the GitHub runner's Java 17 runtime.
      - name: Set up Java for LTeX+
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Verify Java for LTeX+
        shell: bash
        run: |
          set -euo pipefail

          echo "JAVA_HOME=$JAVA_HOME"

          echo "Java executable:"
          command -v java

          echo "Java version:"
          java --version

      # LTeX+ provides grammar and spelling checks for Markdown and MDX.
      # Use the standalone Linux bundle and run it with the Java 21
      # runtime configured above.
      - name: Install LTeX+
        shell: bash
        run: |
          set -euo pipefail

          LTEX_VERSION="18.7.0"
          LTEX_DIR="/opt/ltex-ls-plus"
          LTEX_ARCHIVE="/tmp/ltex-ls-plus.tar.gz"
          LTEX_EXTRACT="/tmp/ltex-ls-plus-extract"

          echo "Downloading LTeX+ ${LTEX_VERSION}..."

          curl -fsSL \
            "https://github.com/ltex-plus/ltex-ls-plus/releases/download/${LTEX_VERSION}/ltex-ls-plus-${LTEX_VERSION}-linux-x64.tar.gz" \
            -o "$LTEX_ARCHIVE"

          echo "Archive contents (first 30 entries):"
          tar -tzf "$LTEX_ARCHIVE" | sed -n '1,30p'

          rm -rf "$LTEX_EXTRACT"
          mkdir -p "$LTEX_EXTRACT"
          tar -xzf "$LTEX_ARCHIVE" -C "$LTEX_EXTRACT"

          echo "Locating ltex-cli-plus..."

          LTEX_CLI="$(
            find "$LTEX_EXTRACT" \
              -type f \
              -path '*/bin/ltex-cli-plus' \
              -print \
              -quit
          )"

          if [ -z "$LTEX_CLI" ]; then
            echo "::error::ltex-cli-plus was not found in the extracted LTeX+ archive."
            echo "Extracted files:"
            find "$LTEX_EXTRACT" -maxdepth 4 -type f | sort | sed -n '1,100p'
            exit 1
          fi

          echo "Found CLI: $LTEX_CLI"

          # bin/ltex-cli-plus -> bin -> installation root
          LTEX_ROOT="$(dirname "$(dirname "$LTEX_CLI")")"
          echo "Detected LTeX+ root: $LTEX_ROOT"

          sudo rm -rf "$LTEX_DIR"
          sudo mkdir -p "$LTEX_DIR"

          # Preserve the complete installation tree because the launcher
          # expects sibling lib/runtime files relative to this root.
          sudo cp -a "$LTEX_ROOT"/. "$LTEX_DIR"/

          sudo chmod +x "$LTEX_DIR/bin/ltex-cli-plus"

          if [ -f "$LTEX_DIR/bin/ltex-ls-plus" ]; then
            sudo chmod +x "$LTEX_DIR/bin/ltex-ls-plus"
          fi

          # GITHUB_PATH applies to subsequent workflow steps.
          echo "$LTEX_DIR/bin" >> "$GITHUB_PATH"

          echo "Installed LTeX+ bin directory:"
          ls -la "$LTEX_DIR/bin"

          echo "Java being used:"
          echo "JAVA_HOME=$JAVA_HOME"
          command -v java
          java --version

          echo "Bundled Java executables, if any:"
          find "$LTEX_DIR" \
            -maxdepth 5 \
            -type f \
            -path '*/bin/java' \
            -print || true

          echo "LTeX+:"
          "$LTEX_DIR/bin/ltex-cli-plus" --version

      # Build the effective LTeX+ configuration used by CI.
      #
      # VS Code stores workspace-folder quick-fix data in these external files:
      #   .vscode/ltex.dictionary.en-GB.txt
      #   .vscode/ltex.disabledRules.en-GB.txt
      #   .vscode/ltex.hiddenFalsePositives.en-GB.txt
      #
      # External setting files are handled by the VS Code client, so the
      # standalone CLI does not automatically import them from .vscode.
      # Convert each file into the language-specific arrays expected by LTeX+
      # and merge them with the committed .ltex.json base configuration.
      - name: Build LTeX+ CI configuration
        shell: bash
        run: |
          set -euo pipefail

          BASE_CONFIG=".ltex.json"
          CI_CONFIG=".ltex-ci.json"

          DICTIONARY_FILE=".vscode/ltex.dictionary.en-GB.txt"
          DISABLED_RULES_FILE=".vscode/ltex.disabledRules.en-GB.txt"
          HIDDEN_FALSE_POSITIVES_FILE=".vscode/ltex.hiddenFalsePositives.en-GB.txt"

          for file in \
            "$BASE_CONFIG" \
            "$DICTIONARY_FILE" \
            "$DISABLED_RULES_FILE" \
            "$HIDDEN_FALSE_POSITIVES_FILE"
          do
            if [ ! -f "$file" ]; then
              echo "::error::Required LTeX+ configuration file was not found: $file"
              exit 1
            fi
          done

          node <<'NODE'
          const fs = require('fs');

          const baseConfigPath = '.ltex.json';
          const ciConfigPath = '.ltex-ci.json';

          const dictionaryPath = '.vscode/ltex.dictionary.en-GB.txt';
          const disabledRulesPath = '.vscode/ltex.disabledRules.en-GB.txt';
          const hiddenFalsePositivesPath =
            '.vscode/ltex.hiddenFalsePositives.en-GB.txt';

          function readLines(path) {
            return fs
              .readFileSync(path, 'utf8')
              .replace(/^\uFEFF/, '')
              .split(/\r?\n/)
              .map((line) => line.trim())
              .filter((line) => line.length > 0);
          }

          const config = JSON.parse(
            fs.readFileSync(baseConfigPath, 'utf8').replace(/^\uFEFF/, '')
          );

          config.ltex ??= {};

          const dictionary = readLines(dictionaryPath);
          const disabledRules = readLines(disabledRulesPath);
          const hiddenFalsePositives = readLines(hiddenFalsePositivesPath);

          // hiddenFalsePositives entries are JSON strings containing rule and
          // sentence information. Validate each source-file line before
          // writing it into the generated LTeX+ configuration.
          hiddenFalsePositives.forEach((entry, index) => {
            try {
              JSON.parse(entry);
            } catch (error) {
              throw new Error(
                `${hiddenFalsePositivesPath}:${index + 1} is not valid JSON: ` +
                  error.message
              );
            }
          });

          config.ltex.dictionary = {
            'en-GB': dictionary
          };

          config.ltex.disabledRules = {
            'en-GB': disabledRules
          };

          config.ltex.hiddenFalsePositives = {
            'en-GB': hiddenFalsePositives
          };

          // configurationTarget is a VS Code client setting that controls
          // where quick fixes are written. It is not needed by the CLI.
          delete config.ltex.configurationTarget;

          fs.writeFileSync(
            ciConfigPath,
            JSON.stringify(config, null, 2) + '\n',
            'utf8'
          );

          console.log(`Dictionary entries: ${dictionary.length}`);
          console.log(`Disabled rules: ${disabledRules.length}`);
          console.log(
            `Hidden false positives: ${hiddenFalsePositives.length}`
          );
          NODE

          echo "Validating generated LTeX+ configuration JSON..."
          python -m json.tool "$CI_CONFIG" >/dev/null
          echo "$CI_CONFIG syntax: OK"

          # Keep .ltex-ci.json for explicit preflight use, and replace only the
          # runner workspace copy of .ltex.json so any existing QA code that
          # reads .ltex.json automatically receives the same effective config.
          cp "$CI_CONFIG" "$BASE_CONFIG"

          echo "Effective .ltex.json prepared for CI."
          echo "Generated LTeX+ settings:"
          node <<'NODE'
          const config = require('./.ltex-ci.json');

          console.log(
            JSON.stringify(
              {
                language: config.ltex?.language,
                enabled: config.ltex?.enabled,
                dictionaryEntries:
                  config.ltex?.dictionary?.['en-GB']?.length ?? 0,
                disabledRules:
                  config.ltex?.disabledRules?.['en-GB']?.length ?? 0,
                hiddenFalsePositives:
                  config.ltex?.hiddenFalsePositives?.['en-GB']?.length ?? 0
              },
              null,
              2
            )
          );
          NODE

      # Validate both the LTeX+ runtime and the effective CI configuration.
      # .ltex-ci.json contains the committed base settings plus the three
      # workspace external setting files converted to en-GB arrays.
      - name: Validate LTeX+ installation and configuration
        shell: bash
        run: |
          set -euo pipefail

          if ! command -v ltex-cli-plus >/dev/null 2>&1; then
            echo "::error::ltex-cli-plus was not found in PATH."
            exit 1
          fi

          if [ ! -f ".ltex.json" ]; then
            echo "::error::.ltex.json was not found at the repository root."
            exit 1
          fi

          echo "Java:"
          echo "JAVA_HOME=$JAVA_HOME"
          command -v java
          java --version

          echo "LTeX+ CLI: $(command -v ltex-cli-plus)"
          ltex-cli-plus --version

          if [ ! -f ".ltex-ci.json" ]; then
            echo "::error::.ltex-ci.json was not generated."
            exit 1
          fi

          echo "Effective .ltex.json configuration found."
          python -m json.tool .ltex.json >/dev/null
          echo ".ltex.json syntax: OK"

          echo "Generated .ltex-ci.json configuration found."
          python -m json.tool .ltex-ci.json >/dev/null
          echo ".ltex-ci.json syntax: OK"

          # Exercise the CLI with the generated British-English CI configuration.
          PREFLIGHT_FILE=".ltex-preflight.md"
          trap 'rm -f "$PREFLIGHT_FILE"' EXIT
          printf '# LTeX+ preflight\n\nThe colour of the centre is blue.\n' > "$PREFLIGHT_FILE"

          set +e
          ltex-cli-plus \
            --client-configuration=.ltex-ci.json \
            "$PREFLIGHT_FILE"
          LTEX_EXIT=$?
          set -e

          if [ "$LTEX_EXIT" -eq 0 ]; then
            echo "LTeX+ installation and configuration: OK"
          elif [ "$LTEX_EXIT" -eq 3 ]; then
            # Exit 3 means the check ran successfully and produced diagnostics.
            echo "LTeX+ installation and configuration: OK (diagnostics produced)"
          else
            echo "::error::LTeX+ preflight failed with exit code $LTEX_EXIT."
            exit "$LTEX_EXIT"
          fi

      # The Python QA script invokes Lychee directly and parses its JSON
      # output into the unified documentation QA report.
      - name: Install Lychee
        shell: bash
        run: |
          sudo snap install lychee

          # Make Lychee available to all following workflow steps.
          echo "/snap/bin" >> "$GITHUB_PATH"

          # Verify the installation immediately using the absolute path.
          /snap/bin/lychee --version

      # Validate the effective Lychee configuration used by the Python QA script.
      # lychee.toml and .lycheeignore are optional; if present, Lychee loads them
      # during this preflight and fails here if the configuration is invalid.
      - name: Validate Lychee configuration
        shell: bash
        run: |
          if [ -f "lychee.toml" ]; then
            echo "lychee.toml configuration found."
          else
            echo "No lychee.toml found; using Lychee defaults plus QA-script options."
          fi

          if [ -f ".lycheeignore" ]; then
            echo ".lycheeignore exclusions found."
          else
            echo "No .lycheeignore found; no repository-level URL exclusions configured."
          fi

          # Exercise the same core options used by scripts/ai_docs_review.py
          # without making a network request. A malformed lychee.toml causes
          # Lychee to fail here before the documentation QA step consumes API credits.
          PREFLIGHT_FILE=".lychee-preflight.md"
          trap 'rm -f "$PREFLIGHT_FILE"' EXIT
          printf '# Lychee configuration preflight\n' > "$PREFLIGHT_FILE"

          lychee \
            --root-dir "$(pwd)" \
            --include '^https?://' \
            --format json \
            --no-progress \
            "$PREFLIGHT_FILE"

          echo "Lychee configuration: OK"

      - name: Verify DeepSeek API key
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        shell: bash
        run: |
          if [ -z "$DEEPSEEK_API_KEY" ]; then
            echo "::error::DEEPSEEK_API_KEY repository secret is not configured."
            exit 1
          fi

          echo "DeepSeek API key is configured."

      # Remove any QA output already present from the checked-out repository or
      # Docusaurus build. This prevents a failed QA run from republishing a stale
      # ai-doc-review.html from an earlier commit.
      - name: Remove stale QA report outputs
        shell: bash
        run: |
          set -euo pipefail
          rm -f ai-doc-review.html ai-doc-review.json .qa-run-start
          rm -rf build/qa

          # Record the start of this QA run. Any report copied into the Pages
          # artifact must be newer than this marker.
          touch .qa-run-start

          echo "Stale QA report outputs removed."
          echo "QA run freshness marker created."

      # continue-on-error is intentional. If the QA thresholds fail,
      # the report is still copied into the Pages build and deployed.
      # The qa-gate job below then restores the failure status.
      - name: Run documentation QA
        id: docs-qa
        continue-on-error: true
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          GITHUB_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          echo "Python:"
          python --version

          echo "Vale:"
          vale --version

          echo "mdx2vast:"
          command -v mdx2vast

          echo "Java:"
          java --version

          echo "LTeX+:"
          ltex-cli-plus --version

          echo "Lychee:"
          lychee --version

          python scripts/ai_docs_review.py \
            --ci \
            --mode per-page \
            --filter full \
            --fail-high 0 \
            --fail-medium 25 \
            --fail-api-errors 0

      # Verify that any generated report belongs to this workflow run. The stale
      # outputs were deleted immediately before QA, and this timestamp check adds
      # a second guard against accidentally publishing an older report.
      - name: Verify QA report freshness
        if: always()
        shell: bash
        run: |
          set -euo pipefail

          if [ -f ai-doc-review.html ]; then
            if [ ai-doc-review.html -nt .qa-run-start ]; then
              echo "HTML QA report is fresh."
            else
              echo "::warning::ai-doc-review.html is not newer than the QA run marker; removing it."
              rm -f ai-doc-review.html
            fi
          else
            echo "No HTML QA report was generated."
          fi

          if [ -f ai-doc-review.json ]; then
            if [ ai-doc-review.json -nt .qa-run-start ]; then
              echo "JSON QA report is fresh."
            else
              echo "::warning::ai-doc-review.json is not newer than the QA run marker; removing it."
              rm -f ai-doc-review.json
            fi
          else
            echo "No JSON QA report was generated."
          fi

      # Keep the source HTML and JSON available as a normal Actions
      # artifact as well as publishing the HTML report under /qa/.
      - name: Upload documentation QA artifact
        id: upload-qa-artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: documentation-review
          path: |
            ai-doc-review.html
            ai-doc-review.json
          if-no-files-found: warn

      # Publish the QA report under a commit-specific filename. /qa/index.html is
      # a small stable loader that fetches version.json with a cache-busting query
      # parameter and redirects to the immutable, SHA-versioned report. This avoids
      # stale QA HTML when GitHub Pages or the browser caches /qa/.
      - name: Add QA report to site
        id: add-qa-report
        if: always()
        shell: bash
        run: |
          set -euo pipefail

          QA_VERSION="${GITHUB_SHA:0:12}"
          REPORT_NAME="report-${QA_VERSION}.html"
          REPORT_PATH="build/qa/${REPORT_NAME}"

          mkdir -p build/qa

          if [ -f ai-doc-review.html ]; then
            cp ai-doc-review.html "$REPORT_PATH"
            echo "QA report copied to $REPORT_PATH."
          else
            echo "::warning::ai-doc-review.html was not generated."

            cat > "$REPORT_PATH" <<'EOF'
          <!doctype html>
          <html lang="en-GB">
          <head>
            <meta charset="utf-8">
            <meta name="viewport" content="width=device-width, initial-scale=1">
            <meta name="robots" content="noindex,nofollow">
            <title>Documentation QA</title>
            <style>
              body {
                max-width: 800px;
                margin: 60px auto;
                padding: 0 24px;
                font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
                line-height: 1.6;
              }

              .error {
                padding: 20px;
                border: 1px solid #dc2626;
                border-radius: 8px;
              }
            </style>
          </head>
          <body>
            <main>
              <h1>Documentation QA</h1>
              <div class="error">
                <h2>Report unavailable</h2>
                <p>
                  The documentation QA workflow did not generate an HTML report.
                </p>
                <p>
                  Check the corresponding GitHub Actions workflow run for details.
                </p>
              </div>
            </main>
          </body>
          </html>
          EOF
          fi

          # A small machine-readable pointer to the report from this deployment.
          # The /qa/ loader requests this file with a unique timestamp query string.
          python - <<'PY'
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path

          sha = os.environ["GITHUB_SHA"]
          version = sha[:12]
          payload = {
              "sha": sha,
              "version": version,
              "report": f"report-{version}.html",
              "generatedAt": datetime.now(timezone.utc).isoformat(timespec="seconds"),
          }

          Path("build/qa/version.json").write_text(
              json.dumps(payload, indent=2) + "\n",
              encoding="utf-8",
          )
          Path("build/qa/deployment-version.txt").write_text(sha + "\n", encoding="utf-8")
          PY

          # Keep /qa/ as the human-friendly URL. The loader itself can safely be
          # cached because it always asks for version.json with a cache-busting
          # query parameter before redirecting to the versioned report.
          cat > build/qa/index.html <<'EOF'
          <!doctype html>
          <html lang="en-GB">
          <head>
            <meta charset="utf-8">
            <meta name="viewport" content="width=device-width, initial-scale=1">
            <meta name="robots" content="noindex,nofollow">
            <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
            <meta http-equiv="Pragma" content="no-cache">
            <meta http-equiv="Expires" content="0">
            <title>Documentation QA</title>
            <style>
              body {
                max-width: 800px;
                margin: 60px auto;
                padding: 0 24px;
                font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
                line-height: 1.6;
              }
            </style>
          </head>
          <body>
            <main>
              <h1>Documentation QA</h1>
              <p id="qa-status">Loading the latest QA report…</p>
              <noscript>
                <p>JavaScript is required to resolve the latest versioned QA report.</p>
              </noscript>
            </main>
            <script>
              (async () => {
                const status = document.getElementById('qa-status');

                try {
                  const response = await fetch(
                    `./version.json?_qa=${Date.now()}`,
                    {cache: 'no-store'},
                  );

                  if (!response.ok) {
                    throw new Error(`HTTP ${response.status}`);
                  }

                  const version = await response.json();

                  if (!version.report || !version.sha) {
                    throw new Error('Invalid QA version metadata');
                  }

                  const target =
                    `./${encodeURIComponent(version.report)}?v=${encodeURIComponent(version.sha)}`;

                  window.location.replace(target);
                } catch (error) {
                  console.error('Unable to resolve the latest QA report:', error);
                  status.textContent =
                    'Unable to load the latest QA report. Refresh the page to try again.';
                }
              })();
            </script>
          </body>
          </html>
          EOF

          # Expose the versioned report path to later jobs and workflow summaries.
          echo "report-path=qa/${REPORT_NAME}" >> "$GITHUB_OUTPUT"
          echo "report-version=${QA_VERSION}" >> "$GITHUB_OUTPUT"

          echo "Stable QA URL: qa/"
          echo "Versioned QA report: qa/${REPORT_NAME}"

      # Ask search engines not to index any generated QA HTML.
      # This is not access control: /qa/ remains a public URL.
      - name: Add noindex to QA report
        if: always()
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          qa_dir = Path("build/qa")
          noindex = '<meta name="robots" content="noindex,nofollow">'

          if not qa_dir.is_dir():
              raise SystemExit(0)

          for path in qa_dir.glob("*.html"):
              content = path.read_text(encoding="utf-8")

              if noindex not in content and "<head>" in content:
                  content = content.replace(
                      "<head>",
                      f"<head>\n    {noindex}",
                      1,
                  )
                  path.write_text(content, encoding="utf-8")
                  print(f"Added noindex to {path}")
          PY

      - name: Verify published QA bundle
        if: always()
        shell: bash
        run: |
          set -euo pipefail

          test -s build/qa/index.html || {
            echo "::error::build/qa/index.html is missing or empty."
            exit 1
          }

          test -s build/qa/version.json || {
            echo "::error::build/qa/version.json is missing or empty."
            exit 1
          }

          REPORT_FILE="$(python - <<'PY'
          import json
          from pathlib import Path

          data = json.loads(Path("build/qa/version.json").read_text(encoding="utf-8"))
          print(data["report"])
          PY
          )"

          test -s "build/qa/$REPORT_FILE" || {
            echo "::error::Versioned QA report build/qa/$REPORT_FILE is missing or empty."
            exit 1
          }

          echo "QA loader: build/qa/index.html"
          echo "QA metadata: build/qa/version.json"
          echo "QA report: build/qa/$REPORT_FILE"

      - name: Add QA information to workflow summary
        if: always()
        shell: bash
        run: |
          {
            echo "## Documentation QA"
            echo ""
            echo "QA step outcome: \`${{ steps.docs-qa.outcome }}\`"
            echo ""

            echo "### Checks"
            echo ""
            echo "- AI documentation review — DeepSeek"
            echo "- Static documentation checks"
            echo "- Vale prose/style checks"
            echo "- LTeX+ grammar/spelling checks — British English (en-GB)"
            echo "- Lychee external HTTP(S) link checks"
            echo ""
            echo "Vale findings are incorporated into the unified report with source \`vale\`."
            echo "Lychee findings are incorporated into the unified report with source \`lychee\`."
            echo "LTeX+ findings are incorporated into the unified report with source \`ltex\`."
            echo ""

            echo "The HTML report is included in this GitHub Pages deployment under \`/qa/\`."
            echo "Versioned report: \`${{ steps.add-qa-report.outputs.report-path }}\`"
            echo "Deployment version: \`${{ steps.add-qa-report.outputs.report-version }}\`"
            echo ""

            if [ -n "${{ steps.upload-qa-artifact.outputs.artifact-url }}" ]; then
              echo "### Original report files"
              echo ""
              echo "[Download documentation-review artifact](${{ steps.upload-qa-artifact.outputs.artifact-url }})"
              echo ""
            fi

            echo "### CI thresholds"
            echo ""
            echo "| Finding | Allowed |"
            echo "|---|---:|"
            echo "| High severity | 0 |"
            echo "| Medium severity | 25 |"
            echo "| API/check errors | 0 |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy site
        id: deployment
        uses: actions/deploy-pages@v5

      - name: Add published URLs to workflow summary
        shell: bash
        run: |
          {
            echo "## GitHub Pages deployment"
            echo ""
            echo "### Portfolio"
            echo ""
            echo "${{ steps.deployment.outputs.page_url }}"
            echo ""
            echo "### Documentation QA"
            echo ""
            echo "Stable URL: ${{ steps.deployment.outputs.page_url }}qa/"
            echo ""
            echo "Versioned report: ${{ steps.deployment.outputs.page_url }}${{ needs.build.outputs.qa-report-path }}"
          } >> "$GITHUB_STEP_SUMMARY"

  # The QA command was allowed to continue so that a failing report
  # could still be published. This final job makes the overall workflow
  # fail when the QA thresholds were exceeded.
  qa-gate:
    name: Enforce documentation QA
    needs:
      - build
      - deploy

    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Check build and QA result
        shell: bash
        run: |
          BUILD_RESULT="${{ needs.build.result }}"
          DEPLOY_RESULT="${{ needs.deploy.result }}"
          QA_OUTCOME="${{ needs.build.outputs.qa-outcome }}"

          echo "Build result: $BUILD_RESULT"
          echo "Deploy result: $DEPLOY_RESULT"
          echo "Documentation QA outcome: $QA_OUTCOME"

          if [ "$BUILD_RESULT" != "success" ]; then
            echo "::error::The build job did not complete successfully."
            exit 1
          fi

          if [ "$DEPLOY_RESULT" != "success" ]; then
            echo "::error::The Pages deployment did not complete successfully."
            exit 1
          fi

          if [ "$QA_OUTCOME" != "success" ]; then
            echo "::error::Documentation QA failed its configured thresholds."
            echo "The QA report was still published so the findings can be reviewed."
            exit 1
          fi

          echo "Documentation QA passed."