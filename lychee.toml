# Retry transient network failures.
max_retries = 5
retry_wait_time = 2

# Avoid hammering one host with simultaneous connections.
host_concurrency = 2
host_request_interval = "250ms"

[hosts."github.com"]
concurrency = 1
request_interval = "500ms"